import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("This space intentionally left blank")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
