#Uncomment the next line to define a global platform for your project
# TODO: Undo? I seem to need this to link with the ads sdk
platform :ios, '12.0'

target 'DemoApp' do
  use_frameworks!
  pod 'Mux-Stats-AVPlayer', :path => '../../'
  
end
